{
  "name": "padel-community-azure-functions",
  "version": "1.0.0",
  "description": "Azure Functions for PadelCommunity",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "azure-functions-core-tools": "^4.0.5413",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0"
  }
}
